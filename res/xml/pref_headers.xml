<preference-headers xmlns:android="http://schemas.android.com/apk/res/android" >

    <!-- These settings headers are only used on tablets. -->

	<header
        android:fragment="${packageName}.${activityClass}$GlobalPreferenceFragment"
        android:title="@string/pref_header_general" />

    <header
        android:fragment="${packageName}.${activityClass}$GpsPreferenceFragment"
        android:title="@string/pref_header_general" />

    <header
        android:fragment="${packageName}.${activityClass}$BluetoothPreferenceFragment"
        android:title="@string/pref_header_bluetooth" />
    
    <header
        android:fragment="${packageName}.${activityClass}$TcpipPreferenceFragment"
        android:title="@string/pref_header_tcpip" />
    
</preference-headers>